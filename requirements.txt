import os
from telegram import Update, ReplyKeyboardMarkup
from telegram.ext import ApplicationBuilder, CommandHandler, MessageHandler, filters, ContextTypes
from datetime import datetime

user_data = {}

main_keyboard = ReplyKeyboardMarkup(
    [["🟢 เข้างาน", "🔴 เลิกงาน"],
     ["🚻 เข้าห้องน้ำ", "🔙 กลับที่นั่ง"],
     ["📋 ดูประวัติ"]],
    resize_keyboard=True
)

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("กรุณาเลือกเมนูจากปุ่มด้านล่าง", reply_markup=main_keyboard)

async def handle_message(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user = update.effective_user
    user_id = user.id
    text = update.message.text
    now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")

    if user_id not in user_data:
        user_data[user_id] = {
            "start_time": None,
            "bathroom_time": None,
            "bathroom_msg_id": None
        }

    data = user_data[user_id]

    if text != "🟢 เข้างาน" and data["start_time"] is None:
        await update.message.reply_text(
            "⚠️ กรุณากด 'เข้างาน' ก่อนใช้งานเมนูอื่น",
            reply_to_message_id=update.message.message_id
        )
        return

    if text == "🟢 เข้างาน":
        if data["bathroom_time"] is not None:
            await update.message.reply_text(
                "❌ คุณยังไม่กลับจากห้องน้ำ",
                reply_to_message_id=data["bathroom_msg_id"]
            )
            return

        data["start_time"] = datetime.now()
        data["bathroom_time"] = None
        data["bathroom_msg_id"] = None

        await update.message.reply_text(
            f"✅ คุณ {user.first_name} เข้างานแล้ว {now}",
            reply_to_message_id=update.message.message_id
        )

    elif text == "🔴 เลิกงาน":
        if data["bathroom_time"] is not None:
            await update.message.reply_text(
                "❌ คุณยังไม่กลับจากห้องน้ำ",
                reply_to_message_id=data["bathroom_msg_id"]
            )
            return

        end_time = datetime.now()
        total_duration = end_time - data["start_time"]

        bathroom_duration = (end_time - data["bathroom_time"]) if data["bathroom_time"] else None
        if bathroom_duration and data["bathroom_msg_id"]:
            total_duration -= bathroom_duration

        await update.message.reply_text(
            f"📋 คุณ {user.first_name} เลิกงานแล้ว\nรวมเวลาทำงาน: {str(total_duration).split('.')[0]}",
            reply_to_message_id=update.message.message_id
        )

        data["start_time"] = None

    elif text == "🚻 เข้าห้องน้ำ":
        if data["bathroom_time"] is not None:
            await update.message.reply_text(
                "❌ คุณยังไม่กลับจากห้องน้ำ",
                reply_to_message_id=data["bathroom_msg_id"]
            )
            return

        data["bathroom_time"] = datetime.now()
        sent = await update.message.reply_text(
            f"🚻 คุณ {user.first_name} เข้าห้องน้ำ {now}",
            reply_to_message_id=update.message.message_id
        )
        data["bathroom_msg_id"] = sent.message_id

    elif text == "🔙 กลับที่นั่ง":
        if data["bathroom_time"] is None:
            await update.message.reply_text("❌ คุณยังไม่ได้เข้าห้องน้ำ")
            return

        back_time = datetime.now()
        bathroom_duration = back_time - data["bathroom_time"]
        await update.message.reply_text(
            f"🔙 คุณ {user.first_name} กลับจากห้องน้ำ ใช้เวลา: {str(bathroom_duration).split('.')[0]}",
            reply_to_message_id=data["bathroom_msg_id"]
        )

        data["bathroom_time"] = None
        data["bathroom_msg_id"] = None

    elif text == "📋 ดูประวัติ":
        await update.message.reply_text("📋 ฟีเจอร์ดูประวัติยังไม่เปิดใช้งาน")

    else:
        await update.message.reply_text("กรุณาเลือกเมนูจากปุ่มด้านล่าง")

# เริ่มต้นบอท
if __name__ == '__main__':
    TOKEN = os.environ.get("7796994967:AAFrF9Dl9eFn8EtHnbKGpXjkt2XJXBuCo6M")  # รับค่า BOT_TOKEN จาก Environment Variables
    if not TOKEN:
        raise ValueError("⚠️ BOT_TOKEN ไม่ได้ถูกกำหนดไว้ใน Environment Variables")

    app = ApplicationBuilder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", start))
    app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
    print("✅ Bot is running...")
    app.run_polling()
